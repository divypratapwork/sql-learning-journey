/*
Problem:
Retrieve all records from customers and orders separately.

Concepts used:
SELECT

Approach:
Run separate queries to understand both tables.

Result:
Displays all records from both tables.
*/

SELECT *
FROM customers;

SELECT *
FROM orders;
